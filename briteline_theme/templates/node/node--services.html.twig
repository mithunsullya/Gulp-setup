{#
/**
* @file
* Template override for service taxonomy.
*/
#}
{% extends "node--page.html.twig" %}
{% block content %}
  <div class="services">
    <div class="layout-container">
      <div class="services__header">
        <div class="services__name">
          {{ node.label }}
        </div>
        {% if content.field_description|render|striptags %}
          <div class="services__description">
            {{ content.field_description }}
          </div>
        {% endif %}
      </div>
      {% if content.field_service_images|render|striptags %}
        <div class="section-carousel">
          {{ content.field_service_images }}
        </div>
      {% endif %}
      {% if content.field_summary|render|striptags %}
        <div class="services__detail-description">
          {{ content.field_summary }}
        </div>
      {% endif %}
    </div>
    <div class="services__contents">
      {{ content.field_contents }}
    </div>
  </div>
{% endblock %}
